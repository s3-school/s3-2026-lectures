# Getting started

## Using pixi 

1. Install pixi from https://pixi.sh/
2. Install dependencies: `pixi install`
3. Live preview: `pixi run serve`
4. Build static site: `pixi run build` (output in `site/`)

